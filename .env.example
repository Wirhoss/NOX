# NOX environment variables
# Copy to .env and fill in values

# Browser config
NOX_HEADLESS=true
# Remote browser WebSocket endpoint (browserless, CDP, etc.)
# NOX_WS_ENDPOINT=ws://localhost:3000/playwright

# Output
# NOX_OUTPUT_DIR=./output
